// A program to test the "pow" function.
#include <stdio.h>
#include <math.h>
int main()
{
    int a = 2, b = 3;
    float c = pow(a, b);
    // float d = sqrt(7);
    float d = sqrt(625);
    printf("%f\t%f\n", c, d);
}