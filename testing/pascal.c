#include <stdio.h>
int main() {
  int n;
  printf("Enter the number of lines: ");
  scanf("%d", &n);
  for (int i = 1; i <= n; i++) {
    int i2 = i;
    for (int i = 1; i <= n - i; i++) {
			printf(" ");
		}
		for(int )
		printf("\n");
  }
  return 0;
}